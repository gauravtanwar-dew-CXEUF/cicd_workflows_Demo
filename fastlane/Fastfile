default_platform(:android)

platform :android do
  lane :build_debug do
    gradle(task: "assembleDebug")
  end

  lane :run_tests do
    gradle(task: "testDebug")
  end

  lane :lint_check do
    gradle(task: "lint")
  end
end
